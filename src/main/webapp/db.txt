CREATE TABLE MEMBER(
	USERID VARCHAR(40) NOT NULL,
	USERPW VARCHAR(40) NOT NULL,
	USERNM VARCHAR(40) NOT NULL,
	USERPHONENUM VARCHAR(40) NOT NULL,
	USEREMAIL VARCHAR(40) NOT NULL,
	CREATEDATE  VARCHAR(40) NOT NULL,
	UPDATEDATE  VARCHAR(40) NOT NULL
);

DROP TABLE MEMBER;

INSERT INTO MEMBER(USERID, USERPW, USERNM, USERPHONENUM, USEREMAIL, CREATEDATE, UPDATEDATE) VALUES('admin1', 'admin1111@', 'admin1', '01099144868' , 'admin1@naver.com', TO_CHAR(SYSDATE, 'YYYY-MM-DD'), TO_CHAR(SYSDATE, 'YYYY-MM-DD'));

SELECT * FROM MEMBER;

COMMIT;